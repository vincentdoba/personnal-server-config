[general]

dbpath = {{ isso_database_path }}

host =
	http://{{ hostname }}
	https://{{ hostname }}

notify = smtp

[smtp]

host = localhost
port = 25
to = {{ isso_smtp_to }}
from = {{ isso_smtp_from }}

[moderation]

enabled = {{ isso_moderation_enabled }}

[server]

#listen = https://0.0.0.0:6789/

[guard]

enabled = {{ isso_guard_status }}
ratelimit = {{ isso_guard_rate_limit }}
direct-reply = {{ isso_guard_direct_reply }}
reply-to-self = {{ isso_guard_reply_to_self }}
